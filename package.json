{
  "name": "payment-examples",
  "version": "0.1.0",
  "private": true,
  "description": "Mono-repo of runnable payment-integration demos (Stripe)",
  "scripts": {
    "dev:stripe": "pnpm --filter stripe-checkout dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
