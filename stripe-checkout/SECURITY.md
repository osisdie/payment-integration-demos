# Security policy

Report sensitive issues (credential leaks in issues, exploits in sample code paths) privately to the repository maintainers rather than filing a public issue.

This repository intentionally contains **tutorial-quality** integrations: do not reuse test keys or webhook secrets outside Stripe test mode, and never commit real `.env` files.
