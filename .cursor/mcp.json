{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--output-dir", "${workspaceFolder}/.playwright-mcp"],
      "env": {
        "PLAYWRIGHT_MCP_OUTPUT_DIR": "${workspaceFolder}/.playwright-mcp"
      }
    }
  }
}
